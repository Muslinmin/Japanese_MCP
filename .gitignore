# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Virtual environments
.venv/
venv/
env/
ENV/

# Environment variables / Secrets
.env
.env.local
.env.*.local
.csv
.xlsx

# IDEs and OS files
.vscode/
.idea/
.DS_Store

# A gcloud SDK install landed here when the installer took its own
# "exec -l /bin/bash" hint as a destination path. ~/.bashrc sources it,
# so it is live — ignored rather than deleted. Worth relocating.
exec -l\ /
